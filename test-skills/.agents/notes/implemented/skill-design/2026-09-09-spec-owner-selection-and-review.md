---
title: Spec 复用已有合同并完成必要评审
status: implemented
created: 2026-09-09
updated: 2026-09-09
approval: 本任务会话认可两类输入、必要评审及实际执行评审方案，并明确要求执行实现修正
verification: 两轴实施审查无实质发现，逐项情境文义核对、技能格式兼容校验、本地链接、差异和记录索引检查通过
---

# Spec 复用已有合同并完成必要评审

## 决定与范围

本任务会话是本次工作项及验收依据，无独立会话链接。补充 [Spec 可选工件决定](./2026-09-08-optional-spec-artifact.md)，保留内容就绪、评审、批准与执行授权分离的既有规则。

[to-spec](../../../../skills/user-invoked/to-spec/SKILL.md) 先定位内容属主：同范围已有 Spec 则更新；充分合同已存在于 Proposal、Issue 等工件时复用，不为调用技能另建副本；仅表达不清则在原属主补充；明确要求独立 Spec 时建立引用或按项目接受流程迁移详细合同归属。

访谈后首次成文可直接使用已确定决策和仓库事实，不要求先补齐其他规划文件。访谈建议不自动成为批准决策，表达缺口可整理，重大决策缺口需保留为阻塞并交由 Planning。

完成内容自检后实际执行必要规范评审。复用仍有效的既有评审证据，检查转述准确性、变更及受影响关系；首次成文审查合同一致性、覆盖、验收和归属。修正可从既有权威解决的问题后复核，重大决策或必要审阅者缺失则明确报告阻塞。独立调用交付评审结果；工程流程中将证据提供给 Plan Review，后者仍负责整体规划集门禁，避免重复已覆盖检查。

不强制新增评审文件或并行代理，不强制独立 Spec 调用完成完整 Plan Review。代价是技能交付必须明确实际评审覆盖，不能用内容自检或“待评审”代替已能执行的评审。

范围为该技能、[后备模板](../../../../skills/user-invoked/to-spec/spec-template.md)、[目录简介](../../../../skills/README.md) 和本仓库记录、索引。其他工程技能、配置和安装副本不变。

## 验收条件

- 已有充分 Plan 合同不默认重复成文；现有 Spec 保留属主和标识。
- 显式独立 Spec 请求有明确引用或迁移关系，不创建竞争权威。
- 访谈后可直接成文，区分确定决策、建议与阻塞。
- 实际执行必要评审，复用有效覆盖，修正后复核；不能仅声明待评审即结束可执行工作。
- 首次成文接受规范评审，工程内审查证据可供 Plan Review 复用但不替代整体门禁。
- 独立调用交付实际评审结果和阻塞；内容就绪、评审、批准、执行授权分别记录。

## 交付与验证

已在技能中加入属主选择和实际评审、修正复核、交付步骤，后备模板增加评审证据，目录简介同步。Standards 与 Spec 独立实施审查均无实质发现。

逐项情境核对通过：已有充分合同复用；同范围 Spec 更新；显式独立文档的引用或迁移；访谈首次成文不先造规划产物；重大决策缺口不自行补全；既有有效审查复用；首次成文与变更实际评审；必要评审受阻如实记录；独立规范交付不等于整体 Plan Review 或执行授权。此为文义核对，未运行代理行为测试。

原 quick_validate.py 不识别既有 disable-model-invocation 扩展字段；保留并单独核验手动调用设置后，仅在临时副本排除此字段进行其余格式检查，通过且未删改目标文件的调用策略。Python 校验使用 UTF-8 模式。本地链接、git diff --check 和 node scripts/decisions/check.mjs 通过；索引由 node scripts/decisions/update-index.mjs 生成。
